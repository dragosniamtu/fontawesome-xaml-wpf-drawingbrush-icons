<Window x:Class="DemoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Font Awesome WPF DrawingBrush Demo"
        Width="1400"
        Height="920"
        MinWidth="900"
        MinHeight="700"
        Background="#FFF4F1EA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="30"
                       FontWeight="SemiBold"
                       Foreground="#FF241F1A"
                       Text="Font Awesome 4 WPF Icon Browser" />
            <TextBlock Margin="0,8,0,0"
                       FontSize="14"
                       Foreground="#FF5A5249"
                       Text="Browse every generated DrawingBrush resource and filter by icon name in real time." />
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,20,0,20"
                Padding="18"
                Background="#FFFFFCF6"
                BorderBrush="#FFD7CFC1"
                BorderThickness="1"
                CornerRadius="18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#FF5A5249"
                               Text="Search icons" />
                    <TextBox Margin="0,8,0,0"
                             Padding="12,10"
                             FontSize="15"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <Border Grid.Column="1"
                        Margin="20,0,0,0"
                        Padding="14,10"
                        VerticalAlignment="Bottom"
                        Background="#FFF0E7D7"
                        CornerRadius="12">
                    <TextBlock FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#FF3E3529"
                               Text="{Binding ResultSummary}" />
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding FilteredIcons}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="170"
                                    Margin="0,0,16,16"
                                    Padding="16"
                                    Background="#FFFFFCF8"
                                    BorderBrush="#FFE2D9CC"
                                    BorderThickness="1"
                                    CornerRadius="16">
                                <StackPanel>
                                    <Border Width="56"
                                            Height="56"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Background="#FFF3EBDD"
                                            CornerRadius="14">
                                        <Viewbox Margin="12">
                                            <Rectangle Width="24"
                                                       Height="24"
                                                       Fill="{Binding Brush}" />
                                        </Viewbox>
                                    </Border>

                                    <TextBlock Margin="0,14,0,0"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="#FF241F1A"
                                               Text="{Binding Name}"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap" />

                                    <TextBlock Margin="0,8,0,0"
                                               FontSize="12"
                                               Foreground="#FF756A5F"
                                               Text="{Binding ResourceKey}"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="#FF5A5249"
                       Text="No icons match the current search.">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasResults}" Value="False">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Grid>
</Window>
